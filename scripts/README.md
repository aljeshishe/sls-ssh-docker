# Rinning
```
apt update && \
apt install curl && \
curl https://raw.githubusercontent.com/aljeshishe/sls-ssh-docker/master/scripts/insatll.sh | sh && \
curl https://raw.githubusercontent.com/aljeshishe/sls-ssh-docker/master/scripts/run.sh | sh
```
