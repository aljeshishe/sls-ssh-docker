# Rinning
```
apt update
apt install curl
curl -sSL https://raw.githubusercontent.com/aljeshishe/sls-ssh-docker/master/scripts/install.sh | bash -s -- --password <password>
curl -sSL https://raw.githubusercontent.com/aljeshishe/sls-ssh-docker/master/scripts/run.sh | bash
```
