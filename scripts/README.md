# Rinning
```
apt update && \
apt install curl && \
curl https://raw.githubusercontent.com/aljeshishe/sls-ssh-docker/master/scripts/install.sh | sh -s --password <password> && \
curl https://raw.githubusercontent.com/aljeshishe/sls-ssh-docker/master/scripts/run.sh | sh
```
